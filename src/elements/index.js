const { Button } = require('./button.element');
const { Input } = require('./input.element');
const { TextArea } = require('./textArea.element')



module.exports = {
  Button,
  Input,
  TextArea,
};